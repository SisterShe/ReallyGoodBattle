///isPositive(value);

// Returns a boolean whether a given value is positive (True), or negative (False).
// Zero ( 0 ) is considered positive.

//  value          = Value to be checked.

// @Return: True if positive, false if negative.

//Creates Variables
var value = argument0;
var boolean = false;

//Checks if positive.
if value >= 0
{
    boolean = true;
}

//Returns value.
return boolean;
