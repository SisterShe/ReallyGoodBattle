///string_number_sign(number);

// Returns the string "+" or "-" depending of wether given number is postive or negative.
// Value 0 is considered positive, so the value "+" will be returned.

//  number             = Number to be checked value of.

// @Return: String "+" or "-" depending on number.

//Creates Variables
var nbr = argument0;
var check = sign(nbr);
var str = "+";

//Checks for negative.
if check = -1
{
    //Updates string.
    str = "-";
}

//Returns String
return str;
