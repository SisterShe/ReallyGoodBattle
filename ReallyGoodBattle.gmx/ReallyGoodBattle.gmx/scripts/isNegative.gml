///isNegative(value);

// Returns a boolean whether a given value is negative (True), or positive (False).
// Zero ( 0 ) is considered positive.

//  value          = Value to be checked.

// @Return: True if negative, false if positive.

//Creates Variables
var value = argument0;
var boolean = false;

//Checks if positive.
if value < 0
{
    boolean = true;
}

//Returns value.
return boolean;
