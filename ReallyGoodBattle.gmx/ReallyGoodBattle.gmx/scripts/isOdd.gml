///isOdd(value);

// Returns wether a given value is odd (True), or even (False).

//  value          = Value to be checked.

// @Return: True if odd, false if even.

//Creates Variables
var value = argument0;

//Returns value.
return (value & 1);
