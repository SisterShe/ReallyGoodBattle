///isEven(value);

// Returns wether a given value is even (True), or odd (False).

//  value          = Value to be checked.

// @Return: True if even, false if odd.

//Creates Variables
var value = argument0;

//Returns value.
return !(value & 1);
