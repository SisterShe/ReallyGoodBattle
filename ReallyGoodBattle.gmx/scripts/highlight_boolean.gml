///highlight_boolean();

// Simple script that returns true mouse is over instance and false if not.

//@Return: Boolean true if mouseover, false if not.

//Checks to see if mouserover.
if position_meeting(mouse_x,mouse_y,self)
{
    return true;
}
else
{
    return false;
}
