///boolean_string(true, test);

// Compares a string to another string. Returns true if the match.

//  true             = The string to compare to.
//  test             = The string to compare.

// @Return: Boolean True if matching, False if not.

//Creates Variables
var string1 = argument0;
var string2 = argument1;
var bool = false;

//Compares strings.
if (string1 == string2)
{
    bool = true;
}

//Returns result.
return bool;
